spring:
  profiles:
    active:
      - dev      #默认dev
---
spring:
  profiles: dev  #开发环境
  application:
    name: springcloud-config-dev
---
spring:
  profiles: test  #测试环境
  application:
    name: springcloud-config-test
